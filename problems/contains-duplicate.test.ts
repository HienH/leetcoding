import { describe, it, expect } from "vitest";
import { containsDuplicate } from "./contains-duplicate";

describe("Contains Duplicate", () => {
  it("example 1", () => {
    expect(containsDuplicate()).toBe(undefined);
  });
});
