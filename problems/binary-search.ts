// Binary Search
//
// Given a sorted (ascending) array of integers `nums` and an integer `target`,
// return the index of `target` in `nums`, or -1 if not present.
// Must run in O(log n) time.
//
// Examples:
//   search([-1,0,3,5,9,12], 9) -> 4
//   search([-1,0,3,5,9,12], 2) -> -1
//
// Constraints:
//   1 <= nums.length <= 10^4

export function search(nums: number[], target: number): number {
  // TODO
  return -1;
}
